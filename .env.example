# Gravenspire — environment variables
# Copy to `.env.local` and fill in real values. Never commit `.env.local`.

# --- Email (not yet wired — added when /ledger page is built) ---
# RESEND_API_KEY=
# LOOPS_API_KEY=
# LOOPS_NEWSLETTER_LIST_ID=
